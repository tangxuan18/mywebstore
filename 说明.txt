商品模糊查询
    1、分类：放在context域里面。load的时候加载分类
    坑：要用config获取servletContext
    2、对前端数据校验
    3、加分页比较复杂，选做（参数放在①地址栏、②放在域里）

    难点：
        前端参数用Product对象封装 如果用户不输入，应该传null，而不应该传0之类的；如果用户输入，又应该传正确的类型
        cid 默认为请选择，而应该传Null
        点上一页下一页要带着查询时的参数（参数放在①地址栏、②放在域里）

    问题：
        数据库查询，实际类型为数字类型，而参数为String类型

登录注册
    难点：
        邮箱激活
        传参注册时间
    bug:
        重新部署后页面异常

订单
    问题：
        为什么两表有外键，能先后插入
            （我说）被依赖的得先插入
    bug
        （debugged）placeOrder的计算总价js代码勾选错误
        转发到myOrders出现页面空白,categoris是empty的，并没有进入mainServlet；shoppingcart页面同时做查询cart和categories也不行，我怀疑不能同时进行来自两个页面的跳转

